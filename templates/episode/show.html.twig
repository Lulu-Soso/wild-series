{% extends 'base.html.twig' %}

{% block title %}Episode
{% endblock %}

{% block body %}
	<h1>Episode</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ episode.id }}</td>
			</tr>
			<tr>
				<th>Title</th>
				<td>{{ episode.title }}</td>
			</tr>
			<tr>
				<th>Number</th>
				<td>{{ episode.number }}</td>
			</tr>
			<tr>
				<th>Synopsis</th>
				<td>{{ episode.synopsis }}</td>
			</tr>
		</tbody>
	</th>
</table>
<a href="{{ path('episode_index') }}">back to list</a>
<a href="{{ path('episode_edit', {'episode_slug': episode.id}) }}">edit</a>
<td>{{episode.season.program.title}}</td>
{{ include('episode/_delete_form.html.twig') }}<a href="{{ path('program_index') }}">Retour à la liste des programmes</a>{% endblock %}
